const testcase = [1,2,3,1]
const swap = (array: number, idx1: number, idx2: number) => {
  const tmp = array[idx1]
}
const dfs = (array: number[], idx: number, path: number[]) => {
  for(let i = idx; i < array.length; i++) {

  }
}